<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.AudioLab"
        tools:targetApi="34">

        <activity
            android:name=".ui.WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.AudioLab">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.MainActivity"
            android:exported="false" />
        <activity android:name=".ui.TrimActivity"
            android:exported="false" />
        <activity android:name=".ui.MergeActivity"
            android:exported="false" />
        <activity android:name=".ui.EffectsActivity"
            android:exported="false" />
        <activity android:name=".ui.ConvertActivity"
            android:exported="false" />
        <activity android:name=".ui.VideoToAudioActivity"
            android:exported="false" />
        <activity android:name=".ui.AudioToVideoActivity"
            android:exported="false" />

    </application>

</manifest>
